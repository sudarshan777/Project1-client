import React, {
    Component
} from 'react';
import Navbar from './Navbar';

class Profile extends Component {
    state = {}
    render() {
        return (

            <
            Navbar / >
        );
    }
}

export default Profile;